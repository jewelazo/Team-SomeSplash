<%= form_for([@category,@category.comments.build]) do |f| %>
    <%= f.text_area :text, class: 'form-control', autofocus: true, placeholder: 'Some respectul comment here.'%>
    <%= f.submit class: 'button_comment', value: 'COMMENT' %>
<% end %>